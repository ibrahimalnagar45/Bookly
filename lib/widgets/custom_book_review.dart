import 'package:flutter/material.dart';

class CustomBookReview extends StatelessWidget {
  const CustomBookReview({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/test_image.png');
  }
}
